{% extends '@fo/shopping/checkout/steps-layout.html.twig' %}

{% block title %}Adresse{% endblock %}

{% block breadcrumb %}
<div class="breadcrumbs">
    <div class="container">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb justify-content-center">
            <li class="breadcrumb-item">Identification</li>
            <li class="breadcrumb-item active" aria-current="page">Adresse</li>
            <li class="breadcrumb-item">Livraison</li>
            <li class="breadcrumb-item">Récapitulatif</li>
            <li class="breadcrumb-item">Paiement</li>
            </ol>
        </nav>
    </div>
</div>
{% endblock breadcrumb %}

{% block main %}
<header class="mb-4">
    <h1>Adresse de livraison</h1>
</header>

{% if address_choice_form|default(false) %}
    {{ form_start(address_choice_form, { 'attr': { 'id': 'addressForm' }}) }}
    {{ form_end(address_choice_form) }}

    <button type="submit" form="addressForm" class="btn btn-primary">{{ 'Continuer'|trans|raw }}</button>
{% else %}
    <a href="{{ path('accountAddress') }}">Ajoutez une addresse</a>
{% endif %}

{% endblock %}
