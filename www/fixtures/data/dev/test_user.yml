Core\Entity\User:
  user_test:
    name: 'Test User'
    uniqueId (unique): '<userUniqueId()>'
    email (unique): 'test@test.tld'
    password_hash: '<userPasswordEncoder("test")>'
    roles: ['ROLE_USER']
    createdAt: '<dateTimeBetween("now", "now")>'
