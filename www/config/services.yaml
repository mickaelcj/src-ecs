# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: 'fr'
    # This parameter defines the codes of the locales (languages) enabled in the application
    app_locales: en|es
    app.notifications.email_sender: 69loloro10@gmail.com

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:               # defines the scalar arguments once and apply them to any service defined/created in this file
            $projectDir: '%kernel.project_dir%' 
            $locales: '%app_locales%'
            $defaultLocale: '%locale%'
            $emailSender: '%app.notifications.email_sender%'
            
    # makes classes in ecs/Security available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    
    Security\:
        resource: '../ecs/Security/*'
        exclude: '../ecs/Security/{DependencyInjection,Entity,Migrations,Tests,Kernel.php}'

    # add more service definitions when explicit configuration is needed
    # # please note that last definitions always *replace* previous ones
    # Apps\Exception\HTTPExceptionListener:
    #     tags:
    #         - { name: kernel.event_listener, event: kernel.exception }

    # app.security.hash.password.listener:
    #     class: App\Security\HashPasswordListener
    #     tags:
    #         - { name: doctrine.event_subscriber }

    # Symfony\Component\HttpFoundation\Session\Storage\Handler\PdoSessionHandler:
    #     arguments:
    #         - !service { class: PDO, factory: 'database_connection:getWrappedConnection' }
    #         # If you get transaction issues (e.g. after login) uncomment the line below
    #         - { lock_mode: 1 }
